Let me explore the workspace to understand the current state and what needs to be done.
Let me examine the key files to understand the project better.
Let me read more files to fully understand the project.
Let me also check the workflows and remaining scripts.
Let me check the workflows and existing m3u files to understand what tools are needed.
Now I have a thorough understanding of the project. Let me formulate a plan.

## Project Summary

**JCTV** is an IPTV project managing M3U playlists with TV channels from multiple countries (Brazil, Argentina, Mexico, Venezuela, USA, etc.) and EPG (Electronic Program Guide) generation.

## Plan

### 1. Delete .bak files
- `lista2.m3u.bak`
- `lista3.m3u.bak`
- `lista5.m3u.bak`

### 2. Apply `aria2` - Replace `curl` downloads with `aria2c`
**Where:** `fix_lista5.py`, `fix_lista5_agora.py`, `gerar_epg.py`
- Replace `subprocess.run(["curl", ...])` for downloading EPG XML files with `aria2c` for faster parallel downloads
- `aria2c` supports multi-connection downloads which is significantly faster for large `.xml.gz` EPG files

### 3. Apply `yt-dlp` - Extract YouTube live streams
**Where:** `NEWSWORLDNOVOS.m3u` contains many YouTube URLs like `https://www.youtube.com/bbcnews/live`, `https://www.youtube.com/cnbc/live`, etc.
- Create a script or modify existing ones to use `yt-dlp` to resolve YouTube live stream URLs into actual `.m3u8` stream URLs
- This replaces brittle manual extraction with a robust tool designed for this

### 4. Apply `crawl4ai` - Replace Selenium where possible
**Where:** `GLOBO.py`, `GLOBO2.py`, `GLOBO3.py`, `LINK.py` all use heavy Selenium/Selenium-Wire for web scraping
- `stream_finder.py` already uses `crawl4ai` - extend its pattern
- Replace Selenium in `GLOBO3.py` (simplest case - just extracts links from Globoplay page) with `crawl4ai`
- Replace Selenium in `LINK.py` for extracting streams from news sites with `crawl4ai`

### Questions for you:

1. Should I create a **single unified script** that does everything (crawl4ai for scraping, yt-dlp for YouTube, aria2 for downloads, cleanup), or modify the **existing individual scripts**?

2. For the YouTube streams in `NEWSWORLDNOVOS.m3u` (BBC News, CNBC, Al Jazeera, etc.) - should I resolve them to direct `.m3u8` URLs using `yt-dlp`, or keep them as YouTube URLs?

3. Should I also refactor `GLOBO.py` (Selenium) to use `crawl4ai`, or only the simpler scripts?
